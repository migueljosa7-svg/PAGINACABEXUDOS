- [ ] Identificar errores exactos de consola (primeros 10-20) y stacktrace
- [ ] Cambiar lógica del mapa para que `followMode` sea real (panTo) y evitar `fitBounds` cuando se está siguiendo
- [ ] Proteger interpolación cuando `routeGeometryForAnim` cambia (cero distancias / t NaN)
- [ ] Validar que CSS vars y Leaflet icon HTML no generen warnings repetidos
- [ ] Probar en runtime: cambiar ruta, play/pause, arrastrar mapa, hacer click en el marcador

